# params.yaml
data_ingestion:
  test_size: 0.2

feature_engineering:
  target_col: Churn

model_training:
  C: 1
  solver: liblinear
  penalty: l1
  class_weight: null   # try 'balanced' as your next experiment given the class imbalance